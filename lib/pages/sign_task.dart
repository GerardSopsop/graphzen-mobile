import 'package:flutter/material.dart';

class SignTask extends StatelessWidget {
  const SignTask({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
